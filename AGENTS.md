# AGENTS.md — 독도 퀴즈앱

## 프로젝트 개요
초등학생용 독도 4지선다 퀴즈 웹앱. 문제를 풀 때마다 정답/오답을 즉시 표시하고 해설을 보여준다.
서버 없이 `index.html`을 브라우저에서 더블클릭해 실행하는 프론트엔드 전용 앱이다.
사용자는 코딩을 모르는 교사이므로, 결과는 항상 브라우저에서 바로 확인할 수 있어야 한다.

## 실행·확인 방법
- 실행: `index.html`을 Chrome에서 연다. 빌드·설치·서버 실행은 없다.
- 확인: 작업 후 `index.html`을 열어 콘솔(F12)에 오류가 없는지, 10문제를 끝까지 풀어 결과 화면까지 도달하는지 직접 확인한다.
- 문법 검사가 필요하면 `node --check script.js` 만 사용한다. (node가 없으면 생략)

## 기술 범위
- HTML, CSS, JavaScript(ES2015+) 순수 파일만 사용한다.
- 외부 라이브러리, CDN, npm, 빌드 도구, 프레임워크, 서버, 백엔드 API를 사용하지 않는다.
- 웹폰트·아이콘도 외부 로드 없이 시스템 폰트와 이모지·유니코드로 해결한다.

## 파일 구조 (이 구조를 유지)
```
index.html   화면 구조. 인라인 <script>·<style> 금지
style.css    모든 스타일
script.js    앱 로직 (화면 전환, 채점, 결과)
data.js      문제 데이터 배열 QUESTIONS 만 정의
```
- 새 파일이 필요하면 만들기 전에 이유를 설명하고 사용자에게 확인한다.

## 데이터 형식
`data.js`의 문제는 아래 형식을 정확히 따른다. 필드 이름을 바꾸지 않는다.
```js
const QUESTIONS = [
  {
    question: "독도는 어느 도에 속해 있을까요?",
    choices: ["강원도", "경상북도", "경상남도", "전라남도"],
    answer: 1,                      // choices의 인덱스 (0부터)
    explanation: "독도는 경상북도 울릉군 울릉읍 독도리에 속합니다."
  }
];
```
- 문제는 초등학생 수준의 사실 위주로 작성하고, 확실하지 않은 내용은 넣지 않는다.
- 새 문제를 추가할 때 기존 문제와 중복되지 않게 한다.

## 코딩 규칙
- UI 문구는 모두 한국어로 쓴다.
- 변수·함수 이름은 영어 camelCase, 파일 상단에 `'use strict';`
- DOM 조작은 `script.js`에서만 한다. `index.html`에 `onclick=` 속성을 쓰지 않는다.
- 정답은 `--color-correct`(초록), 오답은 `--color-wrong`(빨강) CSS 변수로 표시한다.
- 버튼 높이 최소 48px, 본문 글자 최소 18px, 화면 너비 360px에서도 깨지지 않게 한다.
- 새로고침 시 상태가 사라져도 괜찮다. localStorage는 사용자가 요청할 때만 쓴다.

## 경계
✅ 항상 한다
- 수정 후 `index.html`을 열어 전체 흐름(시작 → 10문제 → 결과)을 확인한다.
- 기존에 동작하던 기능이 그대로 동작하는지 함께 확인한다.
- 작업이 끝나면 바꾼 파일과 내용을 3줄 이내로 요약한다.

⚠️ 먼저 묻는다
- 파일을 추가·삭제하거나 파일 구조를 바꿀 때
- `QUESTIONS` 데이터 형식을 바꿀 때
- 5개 이상의 문제를 한 번에 삭제·교체할 때

🚫 하지 않는다
- 외부 라이브러리·CDN·패키지 설치·서버 코드 추가
- 프로젝트 폴더 밖의 파일 읽기·수정
- 사용자가 요청하지 않은 기능 추가
- 확인되지 않은 독도 관련 사실을 문제에 넣기

## 완료 기준
- 콘솔 오류 0개
- 보기를 클릭하면 즉시 정답/오답 표시 + 해설 표시
- 마지막 문제 후 맞힌 개수·점수와 "다시 풀기" 버튼이 보인다
- 위 항목을 직접 확인한 뒤에만 "완료"라고 보고한다
